
import { hasNativeDateInput } from 'datepicker/helpers/has-native-date-input';
import { module, test } from 'qunit';

module('Unit | Helper | has native date input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasNativeDateInput([42]);
  assert.ok(result);
});

